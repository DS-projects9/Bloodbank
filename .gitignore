# dependencies
node_modules/
.expo/
dist/
web-build/

# env & secrets
.env
.env.*
!.env.example
*.pem
serviceAccount*.json
firebase-debug.log

# build
*.js.map
.publish/
app/android/app/build/
app/ios/build/

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Android (Kotlin) build outputs
android/.gradle/
android/app/build/
android/build/
android/.kotlin/
*.apk

# Backend (Ktor) build outputs
backend/build/
backend/.gradle/

# Firebase per-project config (contains API keys; not committed)
google-services.json

# Machine-specific local properties
local.properties

# Emulator / ngrok helper output
firebase-debug.log
firestore-debug.log

# Local credentials (test accounts) — never commit
passwords
passwords*

# Signing keystores — never commit
*.jks
*.keystore